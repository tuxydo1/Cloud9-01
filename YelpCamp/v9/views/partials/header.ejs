<!DOCTYPE html>
<html>
   <head>
      <title>Freedom Paths</title>
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
      <!--Knows where to look for the styleheets folder below because of app.use(..."/public"...) in app.js-->
      <link rel="stylesheet" href="/stylesheets/main.css">
   </head>
   <body>
      <nav class="navbar navbar-default">
   
         <div class="container-fluid">
      
            <div class="navbar-header">
         
               <a class="navbar-brand" href="/">Freedom Paths (v9)</a>
         
            </div>
      
            <div class="collapse navbar-collapse">
      
               <ul class="nav navbar-nav navbar-right">
                  <!--Some ejs logic to hide or show these links as appropriate - using the user object passed in-->
                  <!--Note that we have access to the user object inside this partial because it is passed to the ejs 
                  files that 'include' this partial-->
                  <% if (!currentUser) { %>
                     <li><a href="/login">Login</a></li>
                     <li><a href="/register">Sign Up</a></li>
                  <% } else {%>
                     <li><a href="#">Signed In As <%= currentUser.username %></a></li>
                     <li><a href="/logout">Logout</a></li>
                  <% } %>
               </ul>
      
            </div>
      
         </div>
   
      </nav>   
  